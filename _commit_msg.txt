feat: migrate from Vite to Next.js App Router with Supabase SSR and Zustand

- Replace Vite + React SPA with Next.js 16 App Router (Turbopack)
- Add @supabase/ssr with cookie-based auth and middleware route protection
- Replace hardcoded Supabase keys with NEXT_PUBLIC_* env vars
- Add Zustand store for global UI state (user, mobile menu, assistant)
- Add custom ThemeProvider replacing next-themes (React 19 compatible)
- Install Tailwind CSS 3 with postcss, replacing CDN in index.html
- Add file-based routing: (auth)/ and (app)/ route groups
- Update Sidebar to use usePathname/useRouter instead of props
- Fix SSR-unsafe localStorage access in services and LanguageContext
- Remove Vite config, index.html, App.tsx, src/index.tsx
- Add .next and build artifacts to .gitignore
